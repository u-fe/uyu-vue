import { Meta, Canvas, Story, Preview, ArgsTable } from '@storybook/addon-docs'
import { storyFactory } from '~storybook/util/helpers'
import DocsStyle from '~storybook/util/DocsStyle.mdx'
import Pagination from './Pagination.vue'

<DocsStyle />

<Meta title="UI组件/Pagination（分页）" component={Pagination} />

# Pagination（分页）

**`u-pagination`** 组件 基于 [`v-pagination`](https://vuetifyjs.com/zh-Hans/api/v-pagination/) 封装，扩展了当前页面数量的选取功能

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Pagination },
  template: '<Pagination  @size-change="handleSizeChange" v-bind="$props" />',
  methods: {
    handleSizeChange(v) {
      console.log('asdfasdfa', v)
    },
  },
})

<Preview>
  <Story height="240px" name="基本使用" args={{ total: 2000, totalVisible: 7 }}>
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable of={Pagination} />
